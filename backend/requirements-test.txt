# Testing requirements for SprintDesk backend
pytest==8.4.1
pytest-cov==4.1.0
pytest-asyncio==1.1.0
pytest-mock==3.12.0
httpx==0.28.1
requests==2.31.0

# For test database
# Note: SQLite support is built into Python, no additional package needed